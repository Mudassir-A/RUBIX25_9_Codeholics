## Steps to start the backend API:

1. Create a virtual environment:
```bash
$ python -m venv venv
```
2. Install all dependencies
``` bash
$ pip install -r requirements.txt
```
3. Start server by running `app.py`
```bash
$ run app.py
```